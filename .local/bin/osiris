#!/usr/bin/env bash
# OSIRIS — Sovereign Quantum Copilot (DNA::}{::lang v51.843)
# Launches the REAL OSIRIS CLI (1552 lines, 63+ tools, LLM backends)
# with DNA-Lang physics tools (τ-phase, organism compiler, CRSM validation).
set -euo pipefail

OWNER_HOME="/home/devinpd"
SDK_ROOT="${OWNER_HOME}/Documents/copilot-sdk-dnalang"
BOOTSTRAP="${SDK_ROOT}/dnalang/src/dnalang_sdk/osiris_bootstrap.py"

export HOME="$OWNER_HOME"
export PYTHONPATH="${SDK_ROOT}/dnalang/src:${OWNER_HOME}/osiris_cockpit:${OWNER_HOME}/dnalang-sovereign-copilot-sdk/python/src:${PYTHONPATH:-}"

exec python3 "$BOOTSTRAP" "$@"
